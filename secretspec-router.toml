[project]
name = "nixflix"
revision = "1.0"

# Separate manifest prevents application defaults being inherited by this profile.
[profiles.public_tls]
CLOUDFLARE_DNS_API_TOKEN = { description = "Router Caddy DNS validation token for dalebox.pw", required = true }

[profiles.router_web]
BASIC_AUTH_PASSWORD = { description = "Additional public dashboard login; username nixflix", required = true }
